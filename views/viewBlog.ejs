<!DOCTYPE html>
<html lang="en">
  <head>
    <title>View Blog</title>
    <%- include("./partials/head") %>
  </head>
  <body class="bg-light">
    <%- include("./partials/nav") %>

    <div class="container py-5">
      <div class="row justify-content-center">
        <div class="col-md-10 col-lg-8">
          <div class="card shadow-lg border-0 rounded-4">
            <div class="position-relative" style="overflow: hidden;">
              <img
                src="<%= blog.coverImage %>"
                class="card-img-top"
                alt="Blog Cover Image"
                style="width: 100%; height: auto;"
              />
            </div>
            <div class="card-body p-5">
              <h1 class="card-title mb-4 text-center fw-bold text-primary">
                <%= blog.title %>
              </h1>
              <p class="card-text fs-5" style="white-space: pre-wrap; line-height: 1.8;">
                <%= blog.body %>
              </p>
            </div>
            <div class="d-flex align-items-center mt-3">
              <img src="<%= blog.createdBy.profileImageUrl %>" alt="<%= blog.createdBy.fullName %>" class="rounded-circle me-3" style="width: 50px; height: 50px;">
              <div>
                <p class="mb-0 fw-bold"><%= blog.createdBy.fullName %></p>
                <p class="text-muted small">Posted on <%= new Date(blog.createdAt).toLocaleDateString() %></p>
              </div>
            </div>            
            <div class="card-footer bg-primary text-white text-center py-3">
              <p class="mb-0">Thank you for reading!</p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <%- include("./partials/script") %>
  </body>
</html>
